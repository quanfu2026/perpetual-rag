# Perpetual RAG Benchmark — Pinned Dependencies
# Tested on: Python 3.12, Mac Mini 2014 Intel (no GPU)

# Core retrieval
rank-bm25==0.2.2
jieba==0.42.1
scikit-learn>=1.3.0
numpy<2.0
scipy>=1.11.0

# HuggingFace integration (C-MTEB import + BGE)
datasets>=4.0.0
huggingface_hub>=1.0.0

# Neural embedding (T+ primary method)
sentence-transformers>=5.0.0
transformers<4.50  # version pin for sentence-transformers compat
torch>=2.2.0

# Visualization
matplotlib>=3.9.0

# Statistical reports
pandas>=2.0.0

# Optional: Anthropic API for query expansion (LLM-augmented benchmarks)
anthropic>=0.97.0
